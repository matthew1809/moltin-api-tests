package get

import "fmt"

// PromotionItems is part of the get package, it will fetch all promotion items in a cart
func PromotionItems() {
	fmt.Println("this will get the promotion items in a cart if there are any")
}